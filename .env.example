# Absolute origin of the deployed site, without a trailing slash.
# Used for metadataBase, canonical URLs, robots.txt and sitemap.xml.
# While this variable is unset the site is treated as a development preview:
# crawlers are disallowed and pages are marked noindex.
NEXT_PUBLIC_SITE_URL="https://example.com"
